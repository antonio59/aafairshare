The "Add Category" and "Add Location" screens need significant improvement for consistency and usability. Here's a prompt specifically for these screens:

"Enhance the AAFairShare category and location creation screens with these improvements:

1. Implement a consistent modal design pattern:
   - Convert both screens to proper modal dialogs with semi-transparent overlay
   - Add a clear header with 'X' close button in top-right corner
   - Ensure modals have proper rounded corners and drop shadow for visual hierarchy

2. Standardize the form layout:
   - Use consistent vertical spacing between form elements (24px)
   - Implement proper label-input relationships with matching styling
   - Add visual indicators for required fields
   - Ensure text inputs have consistent height and styling

3. For the category creation specifically:
   - Improve color selection UI with a proper grid layout
   - Add visual indicator for currently selected color
   - Consider adding a small preview of how the category will appear
   - Add common category suggestions (Food, Transport, etc.) for quick selection

4. For the location creation:
   - Add a map preview/integration if possible
   - Implement address auto-completion
   - Consider adding recent or favorite locations for quick selection

5. Improve button styling and placement:
   - Replace text 'Cancel' with a proper outlined button
   - Ensure the 'Save' button uses the same styling as other primary actions
   - Place both buttons side by side at bottom with equal width
   - Add proper hover/active states for better interaction feedback

6. Add helpful context:
   - Include subtle helper text explaining how categories/locations are used
   - Make input validation more helpful with specific error messages

7. Ensure these modals appear properly on top of the main interface without causing page shifts

The goal is to create a more polished, consistent experience across both add/edit screens while ensuring all content is properly contained and visually organized."

These improvements would make the data entry process much more intuitive and consistent with modern mobile UI practices.