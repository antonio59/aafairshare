name: Security Scan (DEPRECATED)

# This workflow is deprecated in favor of the more comprehensive snyk-security.yml
# It is being left in place temporarily for reference but will not run

on:
  workflow_dispatch: # Only manual runs for troubleshooting
  # Removed scheduled and automatic triggers

# Add permissions at workflow level
permissions:
  contents: read
  security-events: write
  issues: write # Needed if creating issues for vulnerabilities

jobs:
  security:
    name: Security Scan (Deprecated)
    # This job will output a warning and exit successfully
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated Workflow Notice
        run: |
          echo "⚠️ WARNING: This security workflow is deprecated!"
          echo "Please use the comprehensive Snyk workflow in snyk-security.yml instead."
          echo "This workflow will be removed in a future update."
          exit 0  # Exit successfully to avoid failing the workflow 